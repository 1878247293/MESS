# SAGEM dependencies
# Recommended Python version: 3.10 or 3.11
#
# Install with:
#   pip install -r requirements.txt
#
# Notes:
# - For GPU environments, you may prefer installing PyTorch separately
#   according to your CUDA version before installing the rest.
# - The scripts/setup.sh script already handles a more opinionated
#   conda + CUDA installation flow.

# Core ML / tensor stack
torch>=2.2,<2.8
torchvision>=0.17,<0.23
torchaudio>=2.2,<2.8

# Scientific computing
numpy>=1.26,<3
pandas>=2.1,<3
scipy>=1.11,<2
scikit-learn>=1.4,<2
joblib>=1.3,<2

# Embedding / transformer stack
sentence-transformers>=2.7,<6
transformers>=4.40,<5
tokenizers>=0.15,<1
safetensors>=0.4,<1
huggingface-hub>=0.23,<1

# ANN / graph / utilities used by the matching pipeline
hnswlib>=0.8,<1
networkx>=3.2,<4
psutil>=5.9,<8

# CLI / logging / progress
tyro>=0.8,<1.0
loguru>=0.7,<1
tqdm>=4.66,<5
pyfunctional>=1.5,<2

# LLM data generation / API access
requests>=2.31,<3
openai>=1.30,<3

# Web UI
gradio>=4.44,<6

# Plotting / analysis
matplotlib>=3.8,<4

# Optional config compatibility
PyYAML>=6.0,<7
